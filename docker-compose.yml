version: "3.8"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    command: node build/monitor/index.js

  client:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    deploy:
      replicas: 3
    command: node build/node/index.js
    depends_on:
      - server